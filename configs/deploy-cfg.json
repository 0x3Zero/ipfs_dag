{
  "ipfs_dag": {
    "modules": [
      {
        "name": "ipfs_dag",
        "path": "artifacts/ipfs_dag.wasm",
        "logger_enabled": true,
        "mounted_binaries": [
          ["ipfs", "/usr/bin/ipfs"],
          ["bash", "/usr/bin/bash"]
        ],
        "max_heap_size": "100 KiB",
        "envs": [["/tmp/vault", "./tmp"]]
      }
    ]
  }
}
